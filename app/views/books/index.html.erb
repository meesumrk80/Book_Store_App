<% content_for :title, "Books" %>

<div class="flex items-center justify-between mb-6">
  <h1 class="text-2xl font-bold text-gray-900">Books</h1>
  <% if policy(Book).new? %>
    <%= link_to "New book", new_admin_book_path,
      class: "inline-flex items-center bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700 text-sm font-medium" %>
  <% end %>
</div>

<%= render "book" %>